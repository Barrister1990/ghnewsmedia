/** Back-compat re-exports — prefer `@/components/shell/PanelChrome` in new code. */
export {
  PANEL_CARD as ADMIN_PANEL_CARD,
  PanelPageHeader as AdminPageHeader,
} from '@/components/shell/PanelChrome';
